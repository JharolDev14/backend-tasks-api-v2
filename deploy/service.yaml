apiVersion: v1
kind: Service
metadata:
  name: backend-tasks-api-v2
  namespace: backend-tasks-api-v2
  labels:
    app: backend-tasks-api-v2
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: backend-tasks-api-v2
