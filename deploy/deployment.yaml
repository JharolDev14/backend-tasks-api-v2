apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-tasks-api-v2
  namespace: backend-tasks-api-v2
  labels:
    app: backend-tasks-api-v2
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend-tasks-api-v2
  template:
    metadata:
      labels:
        app: backend-tasks-api-v2
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "3000"
        prometheus.io/path: "/metrics"
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        fsGroup: 1001
      containers:
        - name: backend-tasks-api-v2
          image: app-image
          ports:
            - containerPort: 3000
              name: http
          envFrom:
            - configMapRef:
                name: backend-tasks-api-v2-config
            - secretRef:
                name: backend-tasks-api-v2-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
